[workspace]
resolver = "2"
members = ["e-token", "e-token-api"]

[workspace.package]
authors = ["MagicBlock Labs <dev@magicblocklabs.xyz>"]
repository = "https://github.com/magicblock-labs/ephemeral-token"
license = "MIT"
edition = "2021"

[workspace.lints.rust.unexpected_cfgs]
level = "warn"
check-cfg = [
    'cfg(target_os, values("solana"))',
    'cfg(feature, values("custom-alloc", "custom-panic", "frozen-abi", "no-entrypoint"))',
]

[workspace.metadata.cli]
solana = "3.1.5"

# Specify Rust toolchains for rustfmt, clippy, and build.
# Any unprovided toolchains default to stable.
[workspace.metadata.toolchains]
format = "nightly-2025-02-16"
lint = "nightly-2025-02-16"

[workspace.metadata.release]
pre-release-commit-message = "Publish {{crate_name}} v{{version}}"
tag-message = "Publish {{crate_name}} v{{version}}"
consolidate-commits = false

[workspace.dependencies]
ephemeral-rollups-pinocchio = { git = "https://github.com/magicblock-labs/ephemeral-rollups-sdk.git", rev = "d2327bca82fff59fb06c8ae87739eae1470ed8fa" }
magicblock-delegation-program-api = { git = "https://github.com/magicblock-labs/delegation-program.git", rev = "b4979e2b9fbb16b0c8a4187dbfe442ef9f0f3357" }

borsh = "1.3"
pinocchio = "0.10.1"
solana-address = "2.0"
pinocchio-log = "0.5.1"
pinocchio-pubkey = "0.3"
pinocchio-system = "0.5"
pinocchio-token-2022 = "0.2.0"
pinocchio-associated-token-account = "0.3"

solana-instruction = "3.0.0"
solana-pubkey = "4.0.0"
solana-program-pack = "3.0.0"
solana-program = "3.0.0"
solana-program-error = "3.0.0"
solana-account = "3.2.0"
solana-account-view = "1.0.0"
solana-program-test = "=3.0.2"
solana-signer = "3.0.0"
solana-keypair = "3.0.1"
solana-transaction = "3.0.1"
solana-system-interface = { version = "2.0.0", features = ["bincode"] }
spl-token-interface = "2.0.0"
